

// page properties:
// - content
// - type

// --------------------------

CREATE TABLE IF NOT EXISTS test02 (id INTEGER PRIMARY KEY, status INT, title TEXT;
INSERT INTO test02 ( status, title ) VALUES ( 0, 'first test');

CREATE TABLE IF NOT EXISTS pages (
	id INTEGER PRIMARY KEY,
	parentid INTEGER,
	status INTEGER,
	layout TEXT,
	layout_current TEXT,
	title TEXT,
	description TEXT,
	notes TEXT,
	type TEXT,
	slug TEXT, 
	path TEXT,
	robots_index INTEGER,
	robots_follow INTEGER,
	in_sitemap INTEGER,
	in_search INTEGER,
	in_menu INTEGER,
	link_target TEXT,
	caching TEXT,
	image TEXT,
	language TEXT,
	date_start TEXT,
	date_end TEXT,
	date_recurisve INTEGER, 
	categories TEXT, 
	tags TEXT
);


